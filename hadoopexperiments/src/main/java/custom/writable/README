
Writable

	- All objects that participate in Mapper & Reducer should implement the interface 'Writable'
	- An implementation Class that is used as Mapper output key (or) Reducer Input key should implement RawComparator
	- A Value Class do not need to implement RawComparator

WritableComparable extends Writable

	- Can be used as Mapper Output key (or) Reducer Input Key